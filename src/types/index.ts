export * from './domain';
export * from './api';
export * from './store';

/**
 * Tab identifiers used by the main Layout navigation.
 * Keep in sync with the `tabs` array in Layout.tsx.
 */
export type TabId = 'dashboard' | 'plans' | 'photos' | 'history' | 'library' | 'analytics';

